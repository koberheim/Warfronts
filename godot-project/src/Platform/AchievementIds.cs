namespace FrontsOfWar.Platform;

// A sample of GDD §12.5's 40 achievements, enough for ProgressionService to
// exercise the UnlockAchievement call path end to end. The full 40-entry
// list is content work for a later milestone.
public static class AchievementIds
{
    public const string FirstMissionWon = "first_mission_won";
    public const string WonWithoutLosingDefenseLine = "won_without_losing_defense_line";
}
