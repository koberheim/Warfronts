namespace FrontsOfWar.Economy;

// The three universal tactical abilities every nation shares (GDD §7.6).
// Doctrines add a fourth, nation-specific ability at M6 — not modeled yet.
public enum AbilityType
{
    ArtilleryStrike,
    Rally,
    EmergencyRepair,
}
