# Production map resources

The standalone map editor saves canonical, versioned `MapDefinition` `.tres`
resources in this directory. Production maps are intentionally not authored
until the editor's rendering and validation phases are complete.

Do not place accepted M3.5 planner JSON exports here; they remain review
artifacts and require the explicit conversion path planned for Phase 12.
